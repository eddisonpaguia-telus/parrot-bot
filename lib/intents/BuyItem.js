const OffersClient = require('../clients/offers')


module.exports = (offer, productList) => {
  console.log('offer: ', offer)
  console.log('product list: ', productList)
  
  
  // const entity = entity.find((entity) => entity.type === 'Product')

  //update cart with offer related to entity
};

